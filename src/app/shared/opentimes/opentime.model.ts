export interface OpentimeModel {
  comp: {
    open: string,
    close: string,
    isOpenToday: boolean,
    isOpenNow: boolean
  },
  priv: {
    open: string,
    close: string,
    isOpenToday: boolean,
    isOpenNow: boolean
  }
}
